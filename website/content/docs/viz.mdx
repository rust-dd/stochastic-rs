---
title: Visualization
description: GridPlotter — Plotly-based grid plotter for quick HTML visualisation of paths, surfaces, and smiles.
category: concept
since: 2.0.0
status: stable
features: []
---

# Visualization

`stochastic-rs-viz` ships **`GridPlotter`** — a thin wrapper over
[Plotly](https://plotly.com) that produces self-contained HTML plots
suitable for notebooks, READMEs, and quick visual checks.

`stochastic-rs-viz` is an unconditional dependency of the umbrella crate
(there is no `viz` cargo feature gating it), re-exported as
`stochastic_rs::visualization`:

```toml
[dependencies]
stochastic-rs-viz = "2.6"
# or via the umbrella (always available, no feature flag needed):
stochastic-rs = "2.6"
```

## Examples

### Sample-path overlay

<Tabs items={['Rust']}>
<Tab value="Rust">
<RustExample path="tests/doctest_viz_paths.rs" />
</Tab>
</Tabs>

### Implied-vol surface heatmap

<Tabs items={['Rust']}>
<Tab value="Rust">
<RustExample path="tests/doctest_viz_iv_surface.rs" />
</Tab>
</Tabs>

## What you get

- Sample-path overlays (single + many paths)
- Distribution histograms with overlaid pdf
- 2-D heatmaps (vol surface, IV grid)
- 3-D surface plots
- Smile / skew comparison panels

For interactive notebooks, prefer the Python bindings — the same data
can be passed straight to `matplotlib`, `plotly.py`, or `bokeh`.
